# Development Environment Variables

# Node environment (triggers development overrides)
NODE_ENV=development

# === DEVELOPMENT AUTH OVERRIDE ===
# Enable mock JWT token generation for local development
# When enabled, /api/v1/dev-auth/* endpoints become available
USE_MOCK_AUTH=true

# === OPTIONAL: Real Azure AD / Okta Credentials ===
# Leave these empty to use mock auth
# Only needed if testing real Azure AD/Okta integration

# Azure AD Tenant ID (optional in dev)
# AZURE_AD_TENANT_ID=your-tenant-id

# Azure AD Client ID (optional in dev)
# AZURE_AD_CLIENT_ID=your-client-id

# Azure AD Client Secret (optional in dev)
# Set to enable real auth, or leave empty to use mock auth
# AZURE_AD_CLIENT_SECRET=your-secret

# JWT Secret (alternative to AZURE_AD_CLIENT_SECRET)
# JWT_SECRET=your-jwt-secret

# === DATABASE ===
# Connection string to Azure SQL or local SQL Server
DATABASE_URL="Server=localhost;Database=routing_data_layer;User Id=sa;Password=YourPassword123;"

# Shadow database for migrations (Prisma)
SHADOW_DATABASE_URL="Server=localhost;Database=routing_data_layer_shadow;User Id=sa;Password=YourPassword123;"

# === API ===
# Frontend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
