# Development Environment Configuration

# API Base URL (proxied through Vite, so use relative path)
VITE_API_URL=/api/v1

# Authentication mode (dev uses mock JWT from backend)
VITE_AUTH_MODE=dev

# Optional: Azure AD (leave empty for dev mode)
# VITE_AZURE_AD_CLIENT_ID=
# VITE_AZURE_AD_TENANT_ID=
